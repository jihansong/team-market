# Node / Playwright
node_modules/
scraper/out/
scraper/urls.txt

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Rendered outputs
output/

# Working files derived from user-uploaded template PDFs (top-level only).
# Only the top-level /templates (rendered preview JPGs of uploaded template
# PDFs) is excluded - render/templates/ (Jinja templates) is kept.
/templates/

# IDE
.vscode/
.idea/
.DS_Store

# Local secrets / config
.env
.env.local
